

function lerNumero (A, B, C) { // a=4, b=5, c=6
  if (A <= 0 || B <= 0 || C <= 0) {
  }
    let r = (B + 5, 5);   
    let s = (B + C, 6);

 
    let d = (r + s) / 2;


    console.log("D : ", + d);
}
